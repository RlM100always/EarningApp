<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EarningApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.EarningApp" parent="Base.Theme.EarningApp" />

    <style name="MyTextInputEditTextStyle" parent="Widget.MaterialComponents.TextInputEditText.OutlinedBox">
        <item name="android:background">@drawable/edittext_border</item>
    </style>

    <style name="AppNavigationView" parent="Widget.Design.NavigationView">
        <!-- Customize text size for items -->
        <item name="android:textSize">25sp</item>


        <!-- Customize icon size for items -->
        <item name="android:maxWidth">300dp</item> <!-- Adjust the value as needed -->
        <item name="android:maxHeight">40dp</item> <!-- Adjust the value as needed -->

        <!-- Add other customizations if needed -->
    </style>
</resources>